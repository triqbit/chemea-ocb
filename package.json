{
  "name": "chemea-ocb-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "hugo server -D",
    "css:build": "npx tailwindcss -i ./assets/css/main.css -o ./assets/css/tailwind.css --minify",
    "build": "npm run css:build && hugo --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.17"
  }
}
